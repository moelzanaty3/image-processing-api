describe('Test Server 1', () => {
  it('validate if test work', () => {
    expect(1).toBe(1)
  })
  describe('Test Server 2', () => {
    it('validate if test work', () => {
      expect(2).toBe(2)
    })
  })
})
